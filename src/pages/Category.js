import React from "react";
// pracę rozpocznamy tutaj, musi się pojawić stan, w którym jest przechowywany aktualny link i wtedy
// następuje fetch.
// rozwarz czy nie stworzyć ustawienia menu w strapim bezpośrednio ażeby menu się samo generowało
// i nie było hard coded

export default function Category() {
  return (
    <div>
      <h1>Fetchowane artykuły</h1>
    </div>
  );
}
